extends ../layout

block content
  h1 Ideas
  p.lead All ideas created.
  hr
  .row
    for idea in ideas || []
      .col-sm-6
        h2 #{idea.title}
        p #{idea.description}
        p
          a.btn.btn-default(href='#', role='button') View details »